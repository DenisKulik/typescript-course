"use strict";
const fetchAuth = (url, method) => {
    return 1;
};
fetchAuth('https://www.google.com', 'GET');
const userItem = {
    id: 1,
    name: 'John',
    age: 25,
    skills: ['JavaScript', 'TypeScript']
};
